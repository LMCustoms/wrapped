name: Release

on:
  push:
    branches: [main]
    paths-ignore:
      - "**.md"
      - ".github/**"

jobs:
  release:
    uses: LMCustoms/.github/.github/workflows/release-semantic.yml@main
    permissions:
      contents: write
      issues: write
      pull-requests: write
    secrets: inherit
